import type { CollectionResponse } from '../types';
export declare const shouldAutoOpenCollectionDocument: ({ allowCreate, allowDelete, collection, folder, }: {
    allowCreate: boolean;
    allowDelete: boolean;
    collection: CollectionResponse;
    folder: {
        fullyQualifiedName: string;
    };
}) => boolean;
