"use client";
import { usePopoverAnchor } from "../popover/popover-anchor.js";
import { useSelectProviderContext } from "./select-context.js";
import { createElement, createHook, forwardRef } from "@ariakit/react-utils";
//#region src/select/select-anchor.tsx
const TagName = "div";
/**
* Returns props to create a `SelectAnchor` component.
* @deprecated Use `useComboboxAnchor` instead.
* @see https://ariakit.com/components/select
* @example
* ```jsx
* const store = useSelectStore();
* const props = useSelectAnchor({ store });
* <Role {...props}>Anchor</Role>
* <Select store={store} />
* <SelectPopover store={store}>Content</SelectPopover>
* ```
*/
const useSelectAnchor = createHook(function useSelectAnchor({ store, ...props }) {
	const context = useSelectProviderContext();
	store = store || context;
	return usePopoverAnchor({
		store,
		...props
	});
});
/**
* Renders an element that acts as the anchor for the
* [`SelectPopover`](https://ariakit.com/reference/select-popover) component.
* @deprecated Use
* [`ComboboxAnchor`](https://ariakit.com/reference/combobox-anchor) instead.
* @see https://ariakit.com/components/select
* @example
* ```jsx {3}
* <SelectProvider>
*   <Select />
*   <SelectAnchor>Anchor</SelectAnchor>
*   <SelectPopover>Content</SelectPopover>
* </SelectProvider>
* ```
*/
const SelectAnchor = forwardRef(function SelectAnchor(props) {
	const htmlProps = useSelectAnchor(props);
	return createElement(TagName, htmlProps);
});
//#endregion
export { SelectAnchor, useSelectAnchor };

//# sourceMappingURL=select-anchor.js.map