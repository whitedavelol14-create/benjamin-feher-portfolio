"use client";
import { SelectItemCheckedContext } from "./select-context.js";
import { useContext } from "react";
//#region src/select/select-item-selected.tsx
/**
* Exposes whether the closest
* [`SelectItem`](https://ariakit.com/reference/select-item) is selected through
* a function child.
*
* As a value component, it doesn't render any DOM elements and therefore
* doesn't accept HTML props.
* @deprecated Use
* [`ComboboxItemSelected`](https://ariakit.com/reference/combobox-item-selected)
* instead.
* @see https://ariakit.com/components/select
* @example
* ```jsx {5-7}
* <SelectProvider>
*   <Select />
*   <SelectPopover>
*     <SelectItem value="Apple">
*       <SelectItemSelected>
*         {(selected) => (selected ? <CheckIcon /> : null)}
*       </SelectItemSelected>
*       Apple
*     </SelectItem>
*   </SelectPopover>
* </SelectProvider>
* ```
*/
function SelectItemSelected({ children }) {
	return children(useContext(SelectItemCheckedContext));
}
//#endregion
export { SelectItemSelected };

//# sourceMappingURL=select-item-selected.js.map