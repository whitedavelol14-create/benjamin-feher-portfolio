import { PopoverDisclosureArrowOptions } from "../popover/popover-disclosure-arrow.js";
import { t as SelectStore } from "../select-store-DRCGwW3T.js";
import { Props } from "@ariakit/react-utils";
import { ElementType } from "react";
//#region src/select/select-arrow.d.ts
declare const TagName = "span";
type TagName = typeof TagName;
/**
 * Returns props to create a `SelectArrow` component.
 * @deprecated Use `useComboboxSelectArrow` instead.
 * @see https://ariakit.com/components/select
 * @example
 * ```jsx
 * const store = useSelectStore();
 * const props = useSelectArrow({ store });
 * <Select store={store}>
 *   {store.value}
 *   <Role {...props} />
 * </Select>
 * <SelectPopover store={store}>
 *   <SelectItem value="Apple" />
 *   <SelectItem value="Orange" />
 * </SelectPopover>
 * ```
 */
export declare const useSelectArrow: import("@ariakit/react-utils").Hook<"span", SelectArrowOptions<"span">>;
/**
 * Renders an arrow pointing to the select popover position. It's usually
 * rendered inside the [`Select`](https://ariakit.com/reference/select)
 * component.
 * @deprecated Use
 * [`ComboboxSelectArrow`](https://ariakit.com/reference/combobox-select-arrow)
 * instead.
 * @see https://ariakit.com/components/select
 * @example
 * ```jsx {4}
 * <SelectProvider>
 *   <Select>
 *     {select.value}
 *     <SelectArrow />
 *   </Select>
 *   <SelectPopover>
 *     <SelectItem value="Apple" />
 *     <SelectItem value="Orange" />
 *   </SelectPopover>
 * </SelectProvider>
 * ```
 */
export declare const SelectArrow: (props: SelectArrowProps) => import("react").ReactElement<unknown, string | import("react").JSXElementConstructor<any>>;
/** @deprecated Use `ComboboxSelectArrowOptions` instead. */
export interface SelectArrowOptions<T extends ElementType = TagName> extends PopoverDisclosureArrowOptions<T> {
  /**
   * Object returned by the
   * [`useSelectStore`](https://ariakit.com/reference/use-select-store) hook. If
   * not provided, the closest [`Select`](https://ariakit.com/reference/select)
   * or [`SelectProvider`](https://ariakit.com/reference/select-provider)
   * components' context will be used.
   */
  store?: SelectStore;
}
/** @deprecated Use `ComboboxSelectArrowProps` instead. */
export type SelectArrowProps<T extends ElementType = TagName> = Props<T, SelectArrowOptions<T>>;
//#endregion
//# sourceMappingURL=select-arrow.d.ts.map