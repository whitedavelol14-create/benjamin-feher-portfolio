import { t as ComboboxStore } from "../combobox-store-HDsRsPSm.js";
import { PopoverDisclosureOptions } from "../popover/popover-disclosure.js";
import { n as CompositeTypeaheadOptions } from "../composite-typeahead-CcgA3PL9.js";
import { Props } from "@ariakit/react-utils";
import { ElementType, KeyboardEvent, SelectHTMLAttributes } from "react";
import { BooleanOrCallback } from "@ariakit/utils";
//#region src/combobox/combobox-select.d.ts
declare const TagName = "button";
type TagName = typeof TagName;
/**
 * Returns props to create a `ComboboxSelect` component.
 * @see https://ariakit.com/components/combobox
 */
export declare const useComboboxSelect: import("@ariakit/react-utils").Hook<"button", ComboboxSelectOptions<"button">>;
/**
 * Renders a custom select element that controls the visibility of a
 * [`ComboboxList`](https://ariakit.com/reference/combobox-list) or
 * [`ComboboxPopover`](https://ariakit.com/reference/combobox-popover).
 *
 * By default, the combobox store's
 * [`selectedValue`](https://ariakit.com/reference/combobox-provider#selectedvalue)
 * state is rendered as the children, followed by a
 * [`ComboboxSelectArrow`](https://ariakit.com/reference/combobox-select-arrow).
 * @example
 * ```jsx {2}
 * <ComboboxProvider>
 *   <ComboboxSelect />
 *   <ComboboxPopover>
 *     <ComboboxItem value="Apple" />
 *     <ComboboxItem value="Orange" />
 *   </ComboboxPopover>
 * </ComboboxProvider>
 * ```
 * @see https://ariakit.com/components/combobox
 */
export declare const ComboboxSelect: (props: ComboboxSelectProps) => import("react").ReactElement<unknown, string | import("react").JSXElementConstructor<any>>;
export interface ComboboxSelectOptions<T extends ElementType = TagName> extends PopoverDisclosureOptions<T>, CompositeTypeaheadOptions<T>, Pick<SelectHTMLAttributes<HTMLSelectElement>, "name" | "form" | "required"> {
  /**
   * Object returned by the
   * [`useComboboxStore`](https://ariakit.com/reference/use-combobox-store)
   * hook. If not provided, the closest
   * [`ComboboxProvider`](https://ariakit.com/reference/combobox-provider)
   * component's context will be used.
   */
  store?: ComboboxStore;
  /**
   * Whether arrow keys show the combobox popover.
   * @default true
   */
  showOnKeyDown?: BooleanOrCallback<KeyboardEvent<HTMLElement>>;
  /**
   * Whether arrow keys move through items while the popover is hidden.
   * @default true
   */
  moveOnKeyDown?: BooleanOrCallback<KeyboardEvent<HTMLElement>>;
}
export type ComboboxSelectProps<T extends ElementType = TagName> = Props<T, ComboboxSelectOptions<T>>;
//#endregion
//# sourceMappingURL=combobox-select.d.ts.map