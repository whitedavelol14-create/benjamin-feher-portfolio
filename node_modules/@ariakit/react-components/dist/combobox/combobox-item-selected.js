"use client";
import { ComboboxItemCheckedContext } from "./combobox-context.js";
import { useContext } from "react";
//#region src/combobox/combobox-item-selected.tsx
/**
* Exposes whether the closest
* [`ComboboxItem`](https://ariakit.com/reference/combobox-item) is selected
* through a function child.
*
* As a value component, it doesn't render any DOM elements and therefore
* doesn't accept HTML props.
* @see https://ariakit.com/components/combobox
* @example
* ```jsx
* <ComboboxItem value="Apple">
*   <ComboboxItemSelected>
*     {(selected) => (selected ? <CheckIcon /> : null)}
*   </ComboboxItemSelected>
*   Apple
* </ComboboxItem>
* ```
*/
function ComboboxItemSelected({ children }) {
	return children(useContext(ComboboxItemCheckedContext));
}
//#endregion
export { ComboboxItemSelected };

//# sourceMappingURL=combobox-item-selected.js.map