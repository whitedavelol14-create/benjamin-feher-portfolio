"use client";
import { useComboboxContext } from "./combobox-context.js";
import { invariant } from "@ariakit/utils";
import { useStoreState } from "@ariakit/react-store";
//#region src/combobox/combobox-selected-value.tsx
function ComboboxSelectedValue({ store, fallback, children } = {}) {
	const context = useComboboxContext();
	store = store || context;
	invariant(store, process.env.NODE_ENV !== "production" && "ComboboxSelectedValue must receive a `store` prop or be wrapped in a ComboboxProvider component.");
	const selectedValue = useStoreState(store, ["selectedValue"], (state) => {
		if (!state.selectedValue.length) return fallback;
		return state.selectedValue;
	});
	if (children) return children(selectedValue || "");
	return selectedValue;
}
//#endregion
export { ComboboxSelectedValue };

//# sourceMappingURL=combobox-selected-value.js.map