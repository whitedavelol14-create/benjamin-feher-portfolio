"use client";
import { withDefaultButtonType } from "../button/utils.js";
import { useComboboxScopedContext } from "./combobox-context.js";
import { usePopoverDismiss } from "../popover/popover-dismiss.js";
import { createElement, createHook, forwardRef } from "@ariakit/react-utils";
//#region src/combobox/combobox-dismiss.tsx
const TagName = "button";
/**
* Returns props to create a `ComboboxDismiss` component.
* @see https://ariakit.com/components/combobox
*/
const useComboboxDismiss = createHook(function useComboboxDismiss({ store, ...props }) {
	const context = useComboboxScopedContext();
	store = store || context;
	props = usePopoverDismiss({
		store,
		...props
	});
	return props;
});
/**
* Renders a button that hides a
* [`ComboboxPopover`](https://ariakit.com/reference/combobox-popover)
* component when clicked.
* @example
* ```jsx {4}
* <ComboboxProvider>
*   <ComboboxSelect />
*   <ComboboxPopover>
*     <ComboboxDismiss />
*     <ComboboxList>
*       <ComboboxItem value="Apple" />
*       <ComboboxItem value="Orange" />
*     </ComboboxList>
*   </ComboboxPopover>
* </ComboboxProvider>
* ```
* @see https://ariakit.com/components/combobox
*/
const ComboboxDismiss = forwardRef(function ComboboxDismiss(props) {
	const htmlProps = useComboboxDismiss(withDefaultButtonType(props));
	return createElement(TagName, htmlProps);
});
//#endregion
export { ComboboxDismiss, useComboboxDismiss };

//# sourceMappingURL=combobox-dismiss.js.map