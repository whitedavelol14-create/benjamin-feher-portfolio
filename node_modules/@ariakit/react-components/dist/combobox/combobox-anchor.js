"use client";
import { usePopoverAnchor } from "../popover/popover-anchor.js";
import { useComboboxProviderContext } from "./combobox-context.js";
import { createElement, createHook, forwardRef } from "@ariakit/react-utils";
//#region src/combobox/combobox-anchor.tsx
const TagName = "div";
/**
* Returns props to create a `ComboboxAnchor` component.
* @see https://ariakit.com/components/combobox
* @example
* ```jsx
* const store = useComboboxStore();
* const props = useComboboxAnchor({ store });
* <Role {...props}>Anchor</Role>
* <Combobox store={store} />
* <ComboboxPopover store={store}>Content</ComboboxPopover>
* ```
*/
const useComboboxAnchor = createHook(function useComboboxAnchor({ store, ...props }) {
	const context = useComboboxProviderContext();
	store = store || context;
	return usePopoverAnchor({
		store,
		...props
	});
});
/**
* Renders an element that acts as the anchor for the
* [`ComboboxPopover`](https://ariakit.com/reference/combobox-popover)
* component.
* @see https://ariakit.com/components/combobox
* @example
* ```jsx {3}
* <ComboboxProvider>
*   <Combobox />
*   <ComboboxAnchor>Anchor</ComboboxAnchor>
*   <ComboboxPopover>Content</ComboboxPopover>
* </ComboboxProvider>
* ```
*/
const ComboboxAnchor = forwardRef(function ComboboxAnchor(props) {
	const htmlProps = useComboboxAnchor(props);
	return createElement(TagName, htmlProps);
});
//#endregion
export { ComboboxAnchor, useComboboxAnchor };

//# sourceMappingURL=combobox-anchor.js.map