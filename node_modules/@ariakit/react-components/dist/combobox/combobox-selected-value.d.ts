import { o as ComboboxStoreSelectedValue, t as ComboboxStore } from "../combobox-store-HDsRsPSm.js";
import { ReactNode } from "react";
import { PickRequired, ToPrimitive } from "@ariakit/utils";
//#region src/combobox/combobox-selected-value.d.ts
type Value = ComboboxStoreSelectedValue;
/**
 * Renders the current
 * [`selectedValue`](https://ariakit.com/reference/use-combobox-store#selectedvalue)
 * state in the combobox store.
 *
 * As a value component, it doesn't render any DOM elements and therefore
 * doesn't accept HTML props. It can receive a `fallback` value or a function
 * child to customize how the value is rendered.
 * @example
 * ```jsx {3}
 * <ComboboxProvider>
 *   <ComboboxSelect>
 *     <ComboboxSelectedValue fallback="Select a fruit" />
 *   </ComboboxSelect>
 *   <ComboboxPopover>
 *     <ComboboxItem value="Apple" />
 *     <ComboboxItem value="Orange" />
 *   </ComboboxPopover>
 * </ComboboxProvider>
 * ```
 * @see https://ariakit.com/components/combobox
 */
export declare function ComboboxSelectedValue<T extends Value = Value>(props: PickRequired<ComboboxSelectedValueProps<T>, "fallback">): T;
export declare function ComboboxSelectedValue(props?: ComboboxSelectedValueProps): Value;
export interface ComboboxSelectedValueProps<T extends Value = Value> {
  /**
   * Object returned by the
   * [`useComboboxStore`](https://ariakit.com/reference/use-combobox-store)
   * hook. If not provided, the closest
   * [`ComboboxSelect`](https://ariakit.com/reference/combobox-select) or
   * [`ComboboxProvider`](https://ariakit.com/reference/combobox-provider)
   * components' context will be used.
   */
  store?: ComboboxStore<T>;
  /**
   * The value to use if the store's `selectedValue` state is empty.
   * @default ""
   */
  fallback?: T;
  /**
   * A function that gets called with the current selected value.
   */
  children?: (value: ToPrimitive<T>) => ReactNode;
}
//#endregion
//# sourceMappingURL=combobox-selected-value.d.ts.map