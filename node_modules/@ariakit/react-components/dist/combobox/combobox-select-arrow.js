"use client";
import { useComboboxContext } from "./combobox-context.js";
import { usePopoverDisclosureArrow } from "../popover/popover-disclosure-arrow.js";
import { createElement, createHook, forwardRef } from "@ariakit/react-utils";
//#region src/combobox/combobox-select-arrow.tsx
const TagName = "span";
/**
* Returns props to create a `ComboboxSelectArrow` component.
* @see https://ariakit.com/components/combobox
*/
const useComboboxSelectArrow = createHook(function useComboboxSelectArrow({ store, ...props }) {
	const context = useComboboxContext();
	store = store || context;
	return usePopoverDisclosureArrow({
		store,
		...props
	});
});
/**
* Renders an arrow pointing to the combobox popover position.
* @see https://ariakit.com/components/combobox
*/
const ComboboxSelectArrow = forwardRef(function ComboboxSelectArrow(props) {
	const htmlProps = useComboboxSelectArrow(props);
	return createElement(TagName, htmlProps);
});
//#endregion
export { ComboboxSelectArrow, useComboboxSelectArrow };

//# sourceMappingURL=combobox-select-arrow.js.map