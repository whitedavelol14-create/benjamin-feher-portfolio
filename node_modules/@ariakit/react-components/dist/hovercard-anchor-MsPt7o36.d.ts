import { n as FocusableOptions } from "./focusable-u2sImvln.js";
import { t as HovercardStore } from "./hovercard-store-BMrm9p-r.js";
import { Props } from "@ariakit/react-utils";
import { ElementType, MouseEvent } from "react";
import { BooleanOrCallback } from "@ariakit/utils";
//#region src/hovercard/__hovercard-trigger.d.ts
declare const TagName$1 = "a";
type TagName$1 = typeof TagName$1;
interface HovercardTriggerOptions<T extends ElementType = TagName$1> extends FocusableOptions<T> {
  /**
   * Object returned by the
   * [`useHovercardStore`](https://ariakit.com/reference/use-hovercard-store)
   * hook.
   */
  store: HovercardStore;
  /**
   * Shows the content element based on the user's _hover intent_ over the
   * trigger element. This behavior purposely ignores mobile touch and
   * unintentional mouse enter events, like those that happen during scrolling.
   *
   * Live examples:
   * - [Navigation Menubar](https://ariakit.com/examples/menubar-navigation)
   * - [Sliding Menu](https://ariakit.com/examples/menu-slide)
   * @default true
   */
  showOnHover?: BooleanOrCallback<MouseEvent<HTMLElement>>;
  /**
   * Whether hovering can show the content element while the trigger element is
   * disabled. Set this to `false` when revealing the content counts as
   * activating the trigger rather than explaining it. An element that declares
   * `aria-disabled` or `disabled` on its own, outside Ariakit props, counts as
   * disabled here too. A disabled trigger that isn't `accessibleWhenDisabled`
   * never shows the content on hover, since the content would then be
   * reachable by pointer users alone; this prop can't turn that back on.
   * @default true
   * @deprecated
   * @private
   */
  unstable_showOnHoverWhenDisabled?: BooleanOrCallback<MouseEvent<HTMLElement>>;
  setAnchorElement?: boolean;
}
//#endregion
//#region src/hovercard/hovercard-anchor.d.ts
declare const TagName = "a";
type TagName = typeof TagName;
/**
 * Returns props to create a `HovercardAnchor` component.
 * @see https://ariakit.com/components/hovercard
 * @example
 * ```jsx
 * const store = useHovercardStore();
 * const props = useHovercardAnchor({ store });
 * <Role {...props} render={<a />}>@username</Role>
 * <Hovercard store={store}>Details</Hovercard>
 * ```
 */
declare const useHovercardAnchor: import("@ariakit/react-utils").Hook<"a", HovercardAnchorOptions<"a">>;
/**
 * Renders an anchor element that will open a
 * [`Hovercard`](https://ariakit.com/reference/hovercard) popup on hover.
 * @see https://ariakit.com/components/hovercard
 * @example
 * ```jsx {2}
 * <HovercardProvider>
 *   <HovercardAnchor>@username</HovercardAnchor>
 *   <Hovercard>Details</Hovercard>
 * </HovercardProvider>
 * ```
 */
declare const HovercardAnchor: (props: HovercardAnchorProps) => import("react").ReactElement<unknown, string | import("react").JSXElementConstructor<any>>;
interface HovercardAnchorOptions<T extends ElementType = TagName> extends Omit<HovercardTriggerOptions<T>, "store" | "setAnchorElement"> {
  /**
   * Object returned by the
   * [`useHovercardStore`](https://ariakit.com/reference/use-hovercard-store)
   * hook. If not provided, the closest
   * [`HovercardProvider`](https://ariakit.com/reference/hovercard-provider)
   * component's context will be used.
   */
  store?: HovercardStore;
}
type HovercardAnchorProps<T extends ElementType = TagName> = Props<T, HovercardAnchorOptions<T>>;
//#endregion
export { useHovercardAnchor as i, HovercardAnchorOptions as n, HovercardAnchorProps as r, HovercardAnchor as t };
//# sourceMappingURL=hovercard-anchor-MsPt7o36.d.ts.map