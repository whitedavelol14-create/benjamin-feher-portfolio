"use client";
import { TagContextProvider, useTagProviderContext } from "./tag-context.js";
import { createElement, createHook, forwardRef, useAttribute, useEvent, useWrapElement } from "@ariakit/react-utils";
import { UndoManager, getClosestFocusable, invariant, isApple, queueBeforeEvent } from "@ariakit/utils";
import { jsx } from "react/jsx-runtime";
import { useStoreState } from "@ariakit/react-store";
//#region src/tag/tag-control.tsx
const TagName = "div";
/**
* Returns props to create a `TagControl` component.
* @see https://ariakit.com/components/tag
* @example
* ```jsx
* const props = useTagControl();
* <Role.div {...props} />
* ```
*/
const useTagControl = createHook(function useTagControl({ store, ...props }) {
	const context = useTagProviderContext();
	store = store || context;
	invariant(store, process.env.NODE_ENV !== "production" && "TagControl must receive a `store` prop or be wrapped in a TagProvider component.");
	const labelElement = useStoreState(store, "labelElement");
	useAttribute(labelElement, "id");
	const labelId = labelElement?.id;
	const onMouseDownProp = props.onMouseDown;
	const onMouseDown = useEvent((event) => {
		onMouseDownProp?.(event);
		if (event.defaultPrevented) return;
		const target = event.target;
		const currentTarget = event.currentTarget;
		const focusableTarget = getClosestFocusable(target);
		if (!(focusableTarget === currentTarget) && currentTarget.contains(focusableTarget)) return;
		const { inputElement } = store.getState();
		queueBeforeEvent(event.currentTarget, "mouseup", () => {
			inputElement?.focus();
		});
	});
	const onKeyDownProp = props.onKeyDown;
	const onKeyDown = useEvent((event) => {
		onKeyDownProp?.(event);
		if (event.defaultPrevented) return;
		const pc = !isApple();
		const z = event.key === "z" || event.key === "Z";
		const mod = pc ? event.ctrlKey : event.metaKey;
		const shiftZ = event.shiftKey && z || pc && event.key === "y";
		if (mod && shiftZ) {
			event.preventDefault();
			UndoManager.redo();
		} else if (mod && z) {
			event.preventDefault();
			UndoManager.undo();
		}
	});
	props = useWrapElement(props, (element) => /* @__PURE__ */ jsx(TagContextProvider, {
		value: store,
		children: element
	}), [store]);
	props = {
		role: "group",
		"aria-labelledby": props["aria-label"] != null ? void 0 : labelId,
		...props,
		onMouseDown,
		onKeyDown
	};
	return props;
});
/**
* Renders a wrapper for the [`TagList`](https://ariakit.com/reference/tag-list)
* and [`TagInput`](https://ariakit.com/reference/tag-input) components. This
* component is typically styled as an input field.
*
* Clicking on this element focuses the input element, and the undo and redo
* keyboard shortcuts are handled here, so both the tags and the input are
* covered by a single element.
*
* The tags and the input are separate widgets for assistive technologies, so
* this component renders a `group` element that keeps them together. The
* [`TagLabel`](https://ariakit.com/reference/tag-label) component provides its
* accessible name.
* @see https://ariakit.com/components/tag
* @example
* ```jsx {3-17}
* <TagProvider>
*   <TagLabel>Invitees</TagLabel>
*   <TagControl>
*     <TagList style={{ display: "contents" }}>
*       <TagValues>
*         {(values) =>
*           values.map((value) => (
*             <Tag key={value} value={value}>
*               {value}
*               <TagRemove />
*             </Tag>
*           ))
*         }
*       </TagValues>
*     </TagList>
*     <TagInput />
*   </TagControl>
* </TagProvider>
* ```
*/
const TagControl = forwardRef(function TagControl(props) {
	const htmlProps = useTagControl(props);
	return createElement(TagName, htmlProps);
});
//#endregion
export { TagControl, useTagControl };

//# sourceMappingURL=tag-control.js.map