"use client";
import { useTagContext } from "./tag-context.js";
import { createElement, createHook, forwardRef, useAttribute, useId, useMergeRefs } from "@ariakit/react-utils";
import { invariant } from "@ariakit/utils";
import { useStoreState } from "@ariakit/react-store";
//#region src/tag/tag-label.tsx
const TagName = "label";
/**
* Returns props to create a `TagLabel` component.
* @see https://ariakit.com/components/tag
* @example
* ```jsx
* const props = useTagLabel();
* <Role.label {...props} />
* ```
*/
const useTagLabel = createHook(function useTagLabel({ store, ...props }) {
	const context = useTagContext();
	store = store || context;
	invariant(store, process.env.NODE_ENV !== "production" && "TagLabel must receive a `store` prop or be wrapped in a TagProvider component.");
	const id = useId(props.id);
	const inputElement = useStoreState(store, "inputElement");
	useAttribute(inputElement, "id");
	props = {
		htmlFor: inputElement?.id,
		...props,
		id,
		ref: useMergeRefs(store.setLabelElement, props.ref)
	};
	return props;
});
/**
* Renders a label element for the tag field. It labels the
* [`TagInput`](https://ariakit.com/reference/tag-input) element and provides
* the accessible name for both the group element rendered by
* [`TagControl`](https://ariakit.com/reference/tag-control) and the listbox
* element rendered by [`TagList`](https://ariakit.com/reference/tag-list).
* @see https://ariakit.com/components/tag
* @example
* ```jsx {2}
* <TagProvider>
*   <TagLabel>Invitees</TagLabel>
*   <TagControl>
*     <TagList style={{ display: "contents" }}>
*       <TagValues>
*         {(values) =>
*           values.map((value) => (
*             <Tag key={value} value={value}>
*               {value}
*               <TagRemove />
*             </Tag>
*           ))
*         }
*       </TagValues>
*     </TagList>
*     <TagInput />
*   </TagControl>
* </TagProvider>
* ```
*/
const TagLabel = forwardRef(function TagLabel(props) {
	const htmlProps = useTagLabel(props);
	return createElement(TagName, htmlProps);
});
//#endregion
export { TagLabel, useTagLabel };

//# sourceMappingURL=tag-label.js.map