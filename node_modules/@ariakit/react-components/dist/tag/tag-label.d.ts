import { t as TagStore } from "../tag-store-CcLMUQXF.js";
import { Options, Props } from "@ariakit/react-utils";
import { ElementType } from "react";
//#region src/tag/tag-label.d.ts
declare const TagName = "label";
type TagName = typeof TagName;
/**
 * Returns props to create a `TagLabel` component.
 * @see https://ariakit.com/components/tag
 * @example
 * ```jsx
 * const props = useTagLabel();
 * <Role.label {...props} />
 * ```
 */
export declare const useTagLabel: import("@ariakit/react-utils").Hook<"label", TagLabelOptions<"label">>;
/**
 * Renders a label element for the tag field. It labels the
 * [`TagInput`](https://ariakit.com/reference/tag-input) element and provides
 * the accessible name for both the group element rendered by
 * [`TagControl`](https://ariakit.com/reference/tag-control) and the listbox
 * element rendered by [`TagList`](https://ariakit.com/reference/tag-list).
 * @see https://ariakit.com/components/tag
 * @example
 * ```jsx {2}
 * <TagProvider>
 *   <TagLabel>Invitees</TagLabel>
 *   <TagControl>
 *     <TagList style={{ display: "contents" }}>
 *       <TagValues>
 *         {(values) =>
 *           values.map((value) => (
 *             <Tag key={value} value={value}>
 *               {value}
 *               <TagRemove />
 *             </Tag>
 *           ))
 *         }
 *       </TagValues>
 *     </TagList>
 *     <TagInput />
 *   </TagControl>
 * </TagProvider>
 * ```
 */
export declare const TagLabel: (props: TagLabelProps) => import("react").ReactElement<unknown, string | import("react").JSXElementConstructor<any>>;
export interface TagLabelOptions<_T extends ElementType = TagName> extends Options {
  /**
   * Object returned by the
   * [`useTagStore`](https://ariakit.com/reference/use-tag-store) hook. If not
   * provided, the closest
   * [`TagProvider`](https://ariakit.com/reference/tag-provider) component's
   * context will be used.
   */
  store?: TagStore;
}
export type TagLabelProps<T extends ElementType = TagName> = Props<T, TagLabelOptions<T>>;
//#endregion
//# sourceMappingURL=tag-label.d.ts.map