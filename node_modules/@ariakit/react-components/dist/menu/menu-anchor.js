"use client";
import { usePopoverAnchor } from "../popover/popover-anchor.js";
import { useMenuProviderContext } from "./menu-context.js";
import { createElement, createHook, forwardRef } from "@ariakit/react-utils";
//#region src/menu/menu-anchor.tsx
const TagName = "div";
/**
* Returns props to create a `MenuAnchor` component.
* @see https://ariakit.com/components/menu
* @example
* ```jsx
* const store = useMenuStore();
* const props = useMenuAnchor({ store });
* <Role {...props}>Anchor</Role>
* <MenuButton store={store}>Menu</MenuButton>
* <Menu store={store}>Content</Menu>
* ```
*/
const useMenuAnchor = createHook(function useMenuAnchor({ store, ...props }) {
	const context = useMenuProviderContext();
	store = store || context;
	return usePopoverAnchor({
		store,
		...props
	});
});
/**
* Renders an element that acts as the anchor for the
* [`Menu`](https://ariakit.com/reference/menu) component.
* @see https://ariakit.com/components/menu
* @example
* ```jsx {3}
* <MenuProvider>
*   <MenuButton>Menu</MenuButton>
*   <MenuAnchor>Anchor</MenuAnchor>
*   <Menu>Content</Menu>
* </MenuProvider>
* ```
*/
const MenuAnchor = forwardRef(function MenuAnchor(props) {
	const htmlProps = useMenuAnchor(props);
	return createElement(TagName, htmlProps);
});
//#endregion
export { MenuAnchor, useMenuAnchor };

//# sourceMappingURL=menu-anchor.js.map