import { PopoverAnchorOptions } from "../popover/popover-anchor.js";
import { t as MenuStore } from "../menu-store-CjKYIHeK.js";
import { Props } from "@ariakit/react-utils";
import { ElementType } from "react";
//#region src/menu/menu-anchor.d.ts
declare const TagName = "div";
type TagName = typeof TagName;
/**
 * Returns props to create a `MenuAnchor` component.
 * @see https://ariakit.com/components/menu
 * @example
 * ```jsx
 * const store = useMenuStore();
 * const props = useMenuAnchor({ store });
 * <Role {...props}>Anchor</Role>
 * <MenuButton store={store}>Menu</MenuButton>
 * <Menu store={store}>Content</Menu>
 * ```
 */
export declare const useMenuAnchor: import("@ariakit/react-utils").Hook<"div", MenuAnchorOptions<"div">>;
/**
 * Renders an element that acts as the anchor for the
 * [`Menu`](https://ariakit.com/reference/menu) component.
 * @see https://ariakit.com/components/menu
 * @example
 * ```jsx {3}
 * <MenuProvider>
 *   <MenuButton>Menu</MenuButton>
 *   <MenuAnchor>Anchor</MenuAnchor>
 *   <Menu>Content</Menu>
 * </MenuProvider>
 * ```
 */
export declare const MenuAnchor: (props: MenuAnchorProps) => import("react").ReactElement<unknown, string | import("react").JSXElementConstructor<any>>;
export interface MenuAnchorOptions<T extends ElementType = TagName> extends PopoverAnchorOptions<T> {
  /**
   * Object returned by the
   * [`useMenuStore`](https://ariakit.com/reference/use-menu-store) hook. If not
   * provided, the closest
   * [`MenuProvider`](https://ariakit.com/reference/menu-provider) component's
   * context will be used.
   */
  store?: MenuStore;
}
export type MenuAnchorProps<T extends ElementType = TagName> = Props<T, MenuAnchorOptions<T>>;
//#endregion
//# sourceMappingURL=menu-anchor.d.ts.map