"use client";
import { useComposite } from "../composite/composite.js";
import { RadioGroupDisabledContext, RadioScopedContextProvider, useRadioProviderContext } from "./radio-context.js";
import { createElement, createHook, forwardRef, useEvent, useWrapElement } from "@ariakit/react-utils";
import { disabledFromProps, invariant, isFocusEventOutside } from "@ariakit/utils";
import { useContext } from "react";
import { jsx } from "react/jsx-runtime";
//#region src/radio/radio-group.tsx
const TagName = "div";
function isCheckedRadio(element) {
	if (!element) return false;
	if (element.tagName === "INPUT") {
		const { type, checked } = element;
		if (type === "radio") return checked;
	}
	if (element.getAttribute("role") !== "radio") return false;
	return element.getAttribute("aria-checked") === "true";
}
function getCheckedRadioId(store) {
	const { renderedItems } = store.getState();
	return renderedItems.find((item) => isCheckedRadio(item.element))?.id;
}
/**
* Returns props to create a `RadioGroup` component.
* @see https://ariakit.com/components/radio
* @example
* ```jsx
* const store = useRadioStore();
* const props = useRadioGroup({ store });
* <Role {...props}>
*   <Radio value="Apple" />
*   <Radio value="Orange" />
* </Role>
* ```
*/
const useRadioGroup = createHook(function useRadioGroup({ store, ...props }) {
	const context = useRadioProviderContext();
	store = store || context;
	const disabled = useContext(RadioGroupDisabledContext) || disabledFromProps(props);
	invariant(store, process.env.NODE_ENV !== "production" && "RadioGroup must receive a `store` prop or be wrapped in a RadioProvider component.");
	props = useWrapElement(props, (element) => /* @__PURE__ */ jsx(RadioScopedContextProvider, {
		value: store,
		children: /* @__PURE__ */ jsx(RadioGroupDisabledContext.Provider, {
			value: disabled,
			children: element
		})
	}), [store, disabled]);
	const onBlurCaptureProp = props.onBlurCapture;
	const onBlurCapture = useEvent((event) => {
		onBlurCaptureProp?.(event);
		if (event.defaultPrevented) return;
		if (!isFocusEventOutside(event)) return;
		const checkedId = getCheckedRadioId(store);
		if (!checkedId) return;
		store.setActiveId(checkedId);
	});
	props = {
		role: "radiogroup",
		...props,
		"aria-disabled": disabled || void 0,
		onBlurCapture
	};
	props = useComposite({
		store,
		...props
	});
	return props;
});
/**
* Renders a [`radiogroup`](https://w3c.github.io/aria/#radiogroup) element that
* manages a group of [`Radio`](https://ariakit.com/reference/radio) elements.
* @see https://ariakit.com/components/radio
* @example
* ```jsx
* <RadioProvider>
*   <RadioGroup>
*     <Radio value="Apple" />
*     <Radio value="Orange" />
*   </RadioGroup>
* </RadioProvider>
* ```
*/
const RadioGroup = forwardRef(function RadioGroup(props) {
	const htmlProps = useRadioGroup(props);
	return createElement(TagName, htmlProps);
});
//#endregion
export { RadioGroup, useRadioGroup };

//# sourceMappingURL=radio-group.js.map