import { Options, Props } from "@ariakit/react-utils";
import { CSSProperties, ElementType } from "react";
//#region src/visually-hidden/visually-hidden.d.ts
declare const TagName = "span";
type TagName = typeof TagName;
/**
 * Returns styles to visually hide an element while keeping it accessible to
 * screen readers.
 */
export declare function getVisuallyHiddenStyle(style?: CSSProperties): CSSProperties;
/**
 * Returns props to create a `VisuallyHidden` component. When applying the props
 * returned by this hook to a component, the component will be visually hidden,
 * but still accessible to screen readers.
 * @see https://ariakit.com/components/visually-hidden
 * @example
 * ```jsx
 * const props = useVisuallyHidden();
 * <a href="#">
 *   Learn more<Role {...props}> about the Solar System</Role>.
 * </a>
 * ```
 */
export declare const useVisuallyHidden: import("@ariakit/react-utils").Hook<"span", VisuallyHiddenOptions<"span">>;
/**
 * Renders an element that's visually hidden, but still accessible to screen
 * readers.
 * @see https://ariakit.com/components/visually-hidden
 * @example
 * ```jsx
 * <a href="#">
 *   Learn more<VisuallyHidden> about the Solar System</VisuallyHidden>.
 * </a>
 * ```
 */
export declare const VisuallyHidden: (props: VisuallyHiddenProps) => import("react").ReactElement<unknown, string | import("react").JSXElementConstructor<any>>;
export interface VisuallyHiddenOptions<_T extends ElementType = TagName> extends Options {}
export type VisuallyHiddenProps<T extends ElementType = TagName> = Props<T, VisuallyHiddenOptions<T>>;
//#endregion
//# sourceMappingURL=visually-hidden.d.ts.map