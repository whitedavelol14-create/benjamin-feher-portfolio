//#region src/dialog/utils/use-previous-mouse-down-ref.d.ts
export interface EventTargets {
  elements: Element[];
  rootTarget: EventTarget | null;
}
/**
 * Returns an element followed by its iframe hosts up to the highest readable
 * document.
 */
export declare function getFrameChain(element: Element): Element[];
/**
 * Returns the full composed path and readable iframe host chain for positive
 * dialog membership, plus a target projected into the reference element's
 * root for outside-tree marks.
 */
export declare function getEventTargets(event: Event, referenceElement?: Element | null): EventTargets;
/**
 * Stores both event-target views from the latest mousedown so opening clicks
 * and drag releases are classified from their mousedown origin.
 */
export declare function usePreviousMouseDownRef(enabled?: boolean, scope?: Window, referenceElement?: Element | null): import("react").RefObject<EventTargets | null>;
//#endregion
//# sourceMappingURL=use-previous-mouse-down-ref.d.ts.map