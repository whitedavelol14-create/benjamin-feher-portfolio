"use strict";Object.defineProperty(exports, "__esModule", {value: true});


















var _TJ2QLEWIcjs = require('./TJ2QLEWI.cjs');

// ../ariakit-react-components/dist/focusable/focusable-context.js
var _react = require('react');
var FocusableContext = _react.createContext.call(void 0, true);

// ../ariakit-react-components/dist/__chunks/CcVRFmzE.js

var accessibleWhenDisabledSymbol = /* @__PURE__ */ Symbol("accessibleWhenDisabled");
function accessibleWhenDisabledFromProps(props) {
  var _a, _b;
  return (_b = props.accessibleWhenDisabled) != null ? _b : (_a = props.onLoadedMetadataCapture) == null ? void 0 : _a[accessibleWhenDisabledSymbol];
}
var trulyDisabledAttribute = "data-truly-disabled";
function trulyDisabledFromElement(element) {
  return element.getAttribute(trulyDisabledAttribute) === "true";
}
function resolvedTrulyDisabledFromElement(element) {
  const value = element.getAttribute(trulyDisabledAttribute);
  if (value === "true") return true;
  if (value === "false") return false;
}
function isCompositeMoveKey(key) {
  return key === "ArrowUp" || key === "ArrowRight" || key === "ArrowDown" || key === "ArrowLeft" || key === "Home" || key === "End" || key === "PageUp" || key === "PageDown";
}
var TagName = "div";
var isSafariBrowser = _TJ2QLEWIcjs.isSafari.call(void 0, );
var nativeTabbableMask = 1;
var supportsDisabledMask = 2;
var defaultElementCapabilities = 3;
var alwaysFocusVisibleInputTypes = [
  "text",
  "search",
  "url",
  "tel",
  "email",
  "password",
  "number",
  "date",
  "month",
  "week",
  "time",
  "datetime",
  "datetime-local"
];
function isAlwaysFocusVisible(element) {
  const { tagName, readOnly, type } = element;
  if (tagName === "TEXTAREA" && !readOnly) return true;
  if (tagName === "SELECT" && !readOnly) return true;
  if (tagName === "INPUT" && !readOnly) return alwaysFocusVisibleInputTypes.includes(type);
  if (element.isContentEditable) return true;
  if (element.getAttribute("role") === "combobox" && element.dataset.name) return true;
  return false;
}
function isNativeTabbable(tagName) {
  if (!tagName) return true;
  return tagName === "button" || tagName === "summary" || tagName === "input" || tagName === "select" || tagName === "textarea" || tagName === "a";
}
function supportsDisabledAttribute(tagName) {
  if (!tagName) return true;
  return tagName === "button" || tagName === "input" || tagName === "select" || tagName === "textarea";
}
function getElementCapabilities(tagName) {
  let capabilities = 0;
  if (isNativeTabbable(tagName)) capabilities |= nativeTabbableMask;
  if (supportsDisabledAttribute(tagName)) capabilities |= supportsDisabledMask;
  return capabilities;
}
function isNativeSubmitControl(element) {
  if (element.tagName === "BUTTON") {
    const { type } = element;
    return type === "submit";
  }
  if (element.tagName === "INPUT") {
    const { type } = element;
    return type === "submit" || type === "image";
  }
  return false;
}
function getTabIndex({ focusable, trulyDisabled, nativeTabbable, supportsDisabled, safariTabIndex, tabIndexProp }) {
  if (!focusable) return tabIndexProp;
  if (trulyDisabled) {
    if (nativeTabbable && !supportsDisabled) return -1;
    return;
  }
  if (nativeTabbable) {
    if (safariTabIndex && tabIndexProp == null) return 0;
    return tabIndexProp;
  }
  return tabIndexProp != null ? tabIndexProp : 0;
}
function useDisableEvent(onEvent, disabled) {
  return _TJ2QLEWIcjs.useEvent.call(void 0, (event) => {
    onEvent == null ? void 0 : onEvent(event);
    if (event.defaultPrevented) return;
    if (disabled) {
      event.stopPropagation();
      event.preventDefault();
    }
  });
}
var hasInstalledGlobalEventListeners = false;
var isKeyboardModality = true;
function onGlobalMouseDown(event) {
  const target = event.target;
  if (_TJ2QLEWIcjs.isElement.call(void 0, target) && !target.hasAttribute("data-focus-visible")) isKeyboardModality = false;
}
function onGlobalKeyDown(event) {
  if (isCompositeMoveKey(event.key)) {
    isKeyboardModality = true;
    return;
  }
  if (event.metaKey) return;
  if (event.ctrlKey) return;
  if (event.altKey) {
    if (!isSafariBrowser) return;
    if (event.key !== "Tab") return;
  }
  isKeyboardModality = true;
}
var useFocusable = _TJ2QLEWIcjs.createHook.call(void 0, function useFocusable2({ focusable = true, accessibleWhenDisabled, autoFocus, onFocusVisible, ...props }) {
  const ref = _react.useRef.call(void 0, null);
  const [parentAccessibleWhenDisabled, metadataProps] = _TJ2QLEWIcjs.useMetadataProps.call(void 0, props, accessibleWhenDisabledSymbol, accessibleWhenDisabled);
  accessibleWhenDisabled != null ? accessibleWhenDisabled : accessibleWhenDisabled = parentAccessibleWhenDisabled;
  _react.useEffect.call(void 0, () => {
    if (!focusable) return;
    if (hasInstalledGlobalEventListeners) return;
    _TJ2QLEWIcjs.addGlobalEventListener.call(void 0, "mousedown", onGlobalMouseDown, true);
    _TJ2QLEWIcjs.addGlobalEventListener.call(void 0, "keydown", onGlobalKeyDown, true);
    hasInstalledGlobalEventListeners = true;
  }, [focusable]);
  const disabled = focusable && _TJ2QLEWIcjs.disabledFromProps.call(void 0, props);
  const trulyDisabled = disabled && !accessibleWhenDisabled;
  const [focusVisible, setFocusVisible] = _react.useState.call(void 0, false);
  const focusVisibleRef = _react.useRef.call(void 0, false);
  const nativeSubmitObserverCleanupRef = _react.useRef.call(void 0, null);
  const cleanupFocusVisible = _TJ2QLEWIcjs.useEvent.call(void 0, (element) => {
    var _a;
    (_a = nativeSubmitObserverCleanupRef.current) == null ? void 0 : _a.call(nativeSubmitObserverCleanupRef);
    nativeSubmitObserverCleanupRef.current = null;
    focusVisibleRef.current = false;
    element == null ? void 0 : element.removeAttribute("data-focus-visible");
  });
  _react.useEffect.call(void 0, () => {
    if (focusable && !trulyDisabled) return;
    cleanupFocusVisible(ref.current);
    if (focusVisible) setFocusVisible(false);
  }, [
    focusable,
    trulyDisabled,
    focusVisible,
    cleanupFocusVisible
  ]);
  _react.useEffect.call(void 0, () => {
    if (!focusable) return;
    if (!focusVisible) return;
    const element = ref.current;
    if (!element) return;
    if (typeof IntersectionObserver === "undefined") return;
    const observer = new IntersectionObserver(() => {
      if (!_TJ2QLEWIcjs.isFocusable.call(void 0, element)) {
        focusVisibleRef.current = false;
        setFocusVisible(false);
      }
    });
    observer.observe(element);
    return () => observer.disconnect();
  }, [focusable, focusVisible]);
  _react.useEffect.call(void 0, () => {
    return () => {
      var _a;
      return (_a = nativeSubmitObserverCleanupRef.current) == null ? void 0 : _a.call(nativeSubmitObserverCleanupRef);
    };
  }, []);
  const onKeyPressCapture = useDisableEvent(props.onKeyPressCapture, disabled);
  const onMouseDownCapture = useDisableEvent(props.onMouseDownCapture, disabled);
  const onClickCapture = useDisableEvent(props.onClickCapture, disabled);
  const onAuxClickCapture = useDisableEvent(props.onAuxClickCapture, disabled);
  const handleFocusVisible = (event, currentTarget) => {
    var _a;
    if (currentTarget) event.currentTarget = currentTarget;
    if (!focusable) return;
    const element = event.currentTarget;
    if (!element) return;
    if (!_TJ2QLEWIcjs.hasFocus.call(void 0, element)) return;
    onFocusVisible == null ? void 0 : onFocusVisible(event);
    if (event.defaultPrevented) return;
    element.dataset.focusVisible = "true";
    focusVisibleRef.current = true;
    if (isNativeSubmitControl(element)) {
      (_a = nativeSubmitObserverCleanupRef.current) == null ? void 0 : _a.call(nativeSubmitObserverCleanupRef);
      nativeSubmitObserverCleanupRef.current = null;
      if (typeof IntersectionObserver !== "undefined") {
        const observer = new IntersectionObserver(() => {
          if (_TJ2QLEWIcjs.isFocusable.call(void 0, element)) return;
          cleanupFocusVisible(element);
        });
        observer.observe(element);
        nativeSubmitObserverCleanupRef.current = () => observer.disconnect();
      }
      return;
    }
    setFocusVisible(true);
  };
  const onKeyDownCaptureProp = props.onKeyDownCapture;
  const onKeyDownCapture = _TJ2QLEWIcjs.useEvent.call(void 0, (event) => {
    onKeyDownCaptureProp == null ? void 0 : onKeyDownCaptureProp(event);
    if (event.defaultPrevented) return;
    if (!focusable) return;
    if (focusVisible) return;
    if (focusVisibleRef.current) return;
    if ((event.metaKey || event.altKey || event.ctrlKey) && !isCompositeMoveKey(event.key)) return;
    if (!_TJ2QLEWIcjs.isSelfTarget.call(void 0, event)) return;
    const element = event.currentTarget;
    const applyFocusVisible = () => handleFocusVisible(event, element);
    _TJ2QLEWIcjs.queueBeforeEvent.call(void 0, element, "focusout", applyFocusVisible);
  });
  const onFocusCaptureProp = props.onFocusCapture;
  const onFocusCapture = _TJ2QLEWIcjs.useEvent.call(void 0, (event) => {
    onFocusCaptureProp == null ? void 0 : onFocusCaptureProp(event);
    if (event.defaultPrevented) return;
    if (!focusable) return;
    if (!_TJ2QLEWIcjs.isSelfTarget.call(void 0, event)) {
      setFocusVisible(false);
      return;
    }
    const element = event.currentTarget;
    const applyFocusVisible = () => handleFocusVisible(event, element);
    if (isKeyboardModality || isAlwaysFocusVisible(event.target)) _TJ2QLEWIcjs.queueBeforeEvent.call(void 0, event.target, "focusout", applyFocusVisible);
    else setFocusVisible(false);
  });
  const onBlurProp = props.onBlur;
  const onBlur = _TJ2QLEWIcjs.useEvent.call(void 0, (event) => {
    onBlurProp == null ? void 0 : onBlurProp(event);
    if (!focusable) return;
    if (!_TJ2QLEWIcjs.isFocusEventOutside.call(void 0, event)) return;
    cleanupFocusVisible(event.currentTarget);
    setFocusVisible(false);
  });
  const autoFocusOnShow = _react.useContext.call(void 0, FocusableContext);
  const autoFocusRef = _TJ2QLEWIcjs.useEvent.call(void 0, (element) => {
    if (!focusable) return;
    if (!autoFocus) return;
    if (!element) return;
    if (!autoFocusOnShow) return;
    queueMicrotask(() => {
      if (_TJ2QLEWIcjs.hasFocus.call(void 0, element)) return;
      if (!_TJ2QLEWIcjs.isFocusable.call(void 0, element)) return;
      element.focus();
    });
  });
  const [elementCapabilities, setElementCapabilities] = _react.useState.call(void 0, defaultElementCapabilities);
  _TJ2QLEWIcjs.useSafeLayoutEffect.call(void 0, () => {
    const element = ref.current;
    if (!element) return;
    const nextCapabilities = getElementCapabilities(element.tagName.toLowerCase());
    if (nextCapabilities === defaultElementCapabilities) return;
    setElementCapabilities(nextCapabilities);
  }, []);
  const nativeTabbable = focusable && !!(elementCapabilities & nativeTabbableMask);
  const supportsDisabled = focusable && !!(elementCapabilities & supportsDisabledMask);
  const [safariTabIndex, setSafariTabIndex] = _react.useState.call(void 0, false);
  if (isSafariBrowser) _react.useEffect.call(void 0, () => {
    if (!focusable) return;
    const element = ref.current;
    if (!element) return;
    const { type } = element;
    const isNativeCheckboxOrRadio = element.tagName === "INPUT" && (type === "checkbox" || type === "radio");
    setSafariTabIndex(_TJ2QLEWIcjs.isButton.call(void 0, element) || isNativeCheckboxOrRadio);
  }, [focusable]);
  const styleProp = props.style;
  const style = _react.useMemo.call(void 0, () => {
    if (trulyDisabled) return {
      pointerEvents: "none",
      ...styleProp
    };
    return styleProp;
  }, [trulyDisabled, styleProp]);
  props = {
    "data-focus-visible": focusable && focusVisible || void 0,
    "data-autofocus": autoFocus || void 0,
    "aria-disabled": disabled || void 0,
    ...props,
    ...metadataProps,
    ref: _TJ2QLEWIcjs.useMergeRefs.call(void 0, ref, autoFocusRef, props.ref),
    style,
    tabIndex: getTabIndex({
      focusable,
      trulyDisabled,
      nativeTabbable,
      supportsDisabled,
      safariTabIndex,
      tabIndexProp: props.tabIndex
    }),
    disabled: supportsDisabled && trulyDisabled ? true : void 0,
    [trulyDisabledAttribute]: disabled ? trulyDisabled : void 0,
    contentEditable: disabled ? void 0 : props.contentEditable,
    onKeyPressCapture,
    onClickCapture,
    onAuxClickCapture,
    onMouseDownCapture,
    onKeyDownCapture,
    onFocusCapture,
    onBlur
  };
  return _TJ2QLEWIcjs.removeUndefinedValues.call(void 0, props);
});
var Focusable = _TJ2QLEWIcjs.forwardRef.call(void 0, function Focusable2(props) {
  const htmlProps = useFocusable(props);
  return _TJ2QLEWIcjs.createElement.call(void 0, TagName, htmlProps);
});









exports.FocusableContext = FocusableContext; exports.accessibleWhenDisabledFromProps = accessibleWhenDisabledFromProps; exports.trulyDisabledFromElement = trulyDisabledFromElement; exports.resolvedTrulyDisabledFromElement = resolvedTrulyDisabledFromElement; exports.isCompositeMoveKey = isCompositeMoveKey; exports.useFocusable = useFocusable; exports.Focusable = Focusable;
