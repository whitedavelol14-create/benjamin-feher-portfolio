"use strict";Object.defineProperty(exports, "__esModule", {value: true});

var _GZOIAEFUcjs = require('./GZOIAEFU.cjs');















var _TJ2QLEWIcjs = require('./TJ2QLEWI.cjs');

// ../ariakit-react-components/dist/button/utils.js
function withDefaultButtonType(props) {
  const { render, type } = props;
  if (render || type !== void 0) return props;
  return {
    ...props,
    type: "button"
  };
}

// ../ariakit-react-components/dist/command/command.js
var _react = require('react');
var TagName = "button";
function isNativeClick(event) {
  if (!event.isTrusted) return false;
  const element = event.currentTarget;
  if (event.key === "Enter") return _TJ2QLEWIcjs.isButton.call(void 0, element) || element.tagName === "SUMMARY" || element.tagName === "A";
  if (event.key === " ") return _TJ2QLEWIcjs.isButton.call(void 0, element) || element.tagName === "SUMMARY" || element.tagName === "INPUT" || element.tagName === "SELECT";
  return false;
}
var symbol = /* @__PURE__ */ Symbol("command");
var useCommand = _TJ2QLEWIcjs.createHook.call(void 0, function useCommand2({ clickOnEnter = true, clickOnSpace = true, ...props }) {
  const ref = _react.useRef.call(void 0, null);
  const [isNativeButton, setIsNativeButton] = _react.useState.call(void 0, false);
  const type = props.type;
  _react.useEffect.call(void 0, () => {
    const element = ref.current;
    if (!element) return;
    const nativeButton = _TJ2QLEWIcjs.isButton.call(void 0, element);
    if (type !== void 0 && nativeButton && element.type === "button") return;
    setIsNativeButton(nativeButton);
  }, [type]);
  const [active, setActive] = _react.useState.call(void 0, false);
  const activeRef = _react.useRef.call(void 0, false);
  const disabled = _TJ2QLEWIcjs.disabledFromProps.call(void 0, props);
  const [isDuplicate, metadataProps] = _TJ2QLEWIcjs.useMetadataProps.call(void 0, props, symbol, true);
  _TJ2QLEWIcjs.useSafeLayoutEffect.call(void 0, () => {
    if (!disabled) return;
    activeRef.current = false;
    setActive(false);
  }, [disabled]);
  const onKeyDownProp = props.onKeyDown;
  const onKeyDown = _TJ2QLEWIcjs.useEvent.call(void 0, (event) => {
    onKeyDownProp == null ? void 0 : onKeyDownProp(event);
    const element = event.currentTarget;
    if (event.defaultPrevented) return;
    if (isDuplicate) return;
    if (disabled) return;
    if (!_TJ2QLEWIcjs.isSelfTarget.call(void 0, event)) return;
    if (_TJ2QLEWIcjs.isTextField.call(void 0, element)) return;
    if (element.isContentEditable) return;
    const isEnter = clickOnEnter && event.key === "Enter";
    const isSpace = clickOnSpace && event.key === " ";
    const shouldPreventEnter = event.key === "Enter" && !clickOnEnter;
    const shouldPreventSpace = event.key === " " && !clickOnSpace;
    if (shouldPreventEnter || shouldPreventSpace) {
      event.preventDefault();
      return;
    }
    if (isEnter || isSpace) {
      const nativeClick = isNativeClick(event);
      if (isEnter) {
        if (!nativeClick) {
          event.preventDefault();
          const { view, ...eventInit } = event;
          const click = () => _TJ2QLEWIcjs.fireClickEvent.call(void 0, element, eventInit);
          if (_TJ2QLEWIcjs.isFirefox.call(void 0, )) _TJ2QLEWIcjs.queueBeforeEvent.call(void 0, element, "keyup", click);
          else queueMicrotask(click);
        }
      } else if (isSpace) {
        activeRef.current = true;
        if (!nativeClick) {
          event.preventDefault();
          setActive(true);
        }
      }
    }
  });
  const onKeyUpProp = props.onKeyUp;
  const onKeyUp = _TJ2QLEWIcjs.useEvent.call(void 0, (event) => {
    onKeyUpProp == null ? void 0 : onKeyUpProp(event);
    if (isDuplicate) return;
    const isSpace = clickOnSpace && event.key === " ";
    if (!activeRef.current || !isSpace) return;
    const nativeClick = isNativeClick(event);
    activeRef.current = false;
    if (!nativeClick) setActive(false);
    if (event.defaultPrevented) return;
    if (!_TJ2QLEWIcjs.isSelfTarget.call(void 0, event)) return;
    if (disabled) return;
    if (event.metaKey) return;
    if (nativeClick) return;
    event.preventDefault();
    const element = event.currentTarget;
    const { view, ...eventInit } = event;
    queueMicrotask(() => _TJ2QLEWIcjs.fireClickEvent.call(void 0, element, eventInit));
  });
  const onBlurProp = props.onBlur;
  const onBlur = _TJ2QLEWIcjs.useEvent.call(void 0, (event) => {
    onBlurProp == null ? void 0 : onBlurProp(event);
    if (!activeRef.current) return;
    activeRef.current = false;
    setActive(false);
  });
  props = {
    "data-active": active || void 0,
    type: isNativeButton ? "button" : void 0,
    ...metadataProps,
    ...props,
    ref: _TJ2QLEWIcjs.useMergeRefs.call(void 0, ref, props.ref),
    onKeyDown,
    onKeyUp,
    onBlur
  };
  props = _GZOIAEFUcjs.useFocusable.call(void 0, props);
  return props;
});
var Command = _TJ2QLEWIcjs.forwardRef.call(void 0, function Command2(props) {
  const htmlProps = useCommand(withDefaultButtonType(props));
  return _TJ2QLEWIcjs.createElement.call(void 0, TagName, htmlProps);
});





exports.withDefaultButtonType = withDefaultButtonType; exports.useCommand = useCommand; exports.Command = Command;
