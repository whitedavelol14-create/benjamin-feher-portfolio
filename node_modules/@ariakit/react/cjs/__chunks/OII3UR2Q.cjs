"use strict";Object.defineProperty(exports, "__esModule", {value: true});


var _I2SBPEXRcjs = require('./I2SBPEXR.cjs');



var _2BQ3OBOWcjs = require('./2BQ3OBOW.cjs');



















var _TJ2QLEWIcjs = require('./TJ2QLEWI.cjs');

// ../ariakit-react-components/dist/composite/composite-group.js
var TagName = "div";
var useCompositeGroup = _TJ2QLEWIcjs.createHook.call(void 0, function useCompositeGroup2({ store, ...props }) {
  props = _2BQ3OBOWcjs.useGroup.call(void 0, props);
  return props;
});
var CompositeGroup = _TJ2QLEWIcjs.forwardRef.call(void 0, function CompositeGroup2(props) {
  const htmlProps = useCompositeGroup(props);
  return _TJ2QLEWIcjs.createElement.call(void 0, TagName, htmlProps);
});

// ../ariakit-react-components/dist/composite/composite-group-label.js
var TagName2 = "div";
var useCompositeGroupLabel = _TJ2QLEWIcjs.createHook.call(void 0, function useCompositeGroupLabel2({ store, ...props }) {
  props = _2BQ3OBOWcjs.useGroupLabel.call(void 0, props);
  return props;
});
var CompositeGroupLabel = _TJ2QLEWIcjs.forwardRef.call(void 0, function CompositeGroupLabel2(props) {
  const htmlProps = useCompositeGroupLabel(props);
  return _TJ2QLEWIcjs.createElement.call(void 0, TagName2, htmlProps);
});

// ../ariakit-react-components/dist/composite/composite-hover.js
var _react = require('react');
var TagName3 = "div";
function hoveringInside(event) {
  const nextElement = event.relatedTarget;
  if (!_TJ2QLEWIcjs.isElement.call(void 0, nextElement)) return false;
  return _TJ2QLEWIcjs.contains.call(void 0, event.currentTarget, nextElement);
}
var symbol = /* @__PURE__ */ Symbol("composite-hover");
function movingToAnotherItem(event) {
  const { relatedTarget } = event;
  if (!_TJ2QLEWIcjs.isElement.call(void 0, relatedTarget)) return false;
  let dest = relatedTarget;
  do {
    if (_TJ2QLEWIcjs.hasOwnProperty.call(void 0, dest, symbol) && dest[symbol]) return true;
    dest = dest.parentElement;
  } while (dest);
  return false;
}
var useCompositeHover = _TJ2QLEWIcjs.createHook.call(void 0, function useCompositeHover2({ store, focusOnHover = true, blurOnHoverEnd = !!focusOnHover, ...props }) {
  const context = _I2SBPEXRcjs.useCompositeScopedContext.call(void 0, );
  store = store || context;
  _TJ2QLEWIcjs.invariant.call(void 0, store, process.env.NODE_ENV !== "production" && "CompositeHover must be wrapped in a Composite component.");
  const isMouseMoving = _TJ2QLEWIcjs.useIsMouseMoving.call(void 0, );
  const onMouseMoveProp = props.onMouseMove;
  const focusOnHoverProp = _TJ2QLEWIcjs.useBooleanEvent.call(void 0, focusOnHover);
  const onMouseMove = _TJ2QLEWIcjs.useEvent.call(void 0, (event) => {
    onMouseMoveProp == null ? void 0 : onMouseMoveProp(event);
    if (event.defaultPrevented) return;
    if (!isMouseMoving()) return;
    if (!focusOnHoverProp(event)) return;
    if (!_TJ2QLEWIcjs.hasFocusWithin.call(void 0, event.currentTarget)) {
      const compositeElement = store == null ? void 0 : store.getState().compositeElement;
      if (compositeElement && !_TJ2QLEWIcjs.hasFocus.call(void 0, compositeElement)) compositeElement.focus({ preventScroll: true });
    }
    store == null ? void 0 : store.setActiveId(event.currentTarget.id);
  });
  const onMouseLeaveProp = props.onMouseLeave;
  const blurOnHoverEndProp = _TJ2QLEWIcjs.useBooleanEvent.call(void 0, blurOnHoverEnd);
  const onMouseLeave = _TJ2QLEWIcjs.useEvent.call(void 0, (event) => {
    var _a;
    onMouseLeaveProp == null ? void 0 : onMouseLeaveProp(event);
    if (event.defaultPrevented) return;
    if (!isMouseMoving()) return;
    if (hoveringInside(event)) return;
    if (movingToAnotherItem(event)) return;
    if (!focusOnHoverProp(event)) return;
    if (!blurOnHoverEndProp(event)) return;
    store == null ? void 0 : store.setActiveId(null);
    (_a = store == null ? void 0 : store.getState().compositeElement) == null ? void 0 : _a.focus({ preventScroll: true });
  });
  const ref = _react.useCallback.call(void 0, (element) => {
    if (!element) return;
    element[symbol] = true;
  }, []);
  props = {
    ...props,
    ref: _TJ2QLEWIcjs.useMergeRefs.call(void 0, ref, props.ref),
    onMouseMove,
    onMouseLeave
  };
  return props;
});
var CompositeHover = _TJ2QLEWIcjs.memo.call(void 0, _TJ2QLEWIcjs.forwardRef.call(void 0, function CompositeHover2(props) {
  const htmlProps = useCompositeHover(props);
  return _TJ2QLEWIcjs.createElement.call(void 0, TagName3, htmlProps);
}));

// ../ariakit-react-components/dist/composite/composite-typeahead.js
var TagName4 = "div";
var typeaheadStates = /* @__PURE__ */ new WeakMap();
function getTypeaheadState(store) {
  let typeaheadState = typeaheadStates.get(store);
  if (!typeaheadState) {
    typeaheadState = {
      chars: "",
      cleanupTimeout: 0
    };
    typeaheadStates.set(store, typeaheadState);
  }
  return typeaheadState;
}
function clearChars(typeaheadState) {
  typeaheadState.chars = "";
}
function isValidTypeaheadEvent(event, typeaheadState) {
  const target = event.target;
  if (target && _TJ2QLEWIcjs.isTextField.call(void 0, target)) return false;
  if (event.key === " " && typeaheadState.chars.length) return true;
  return event.key.length === 1 && !event.ctrlKey && !event.altKey && !event.metaKey && /^[\p{Letter}\p{Number}]$/u.test(event.key);
}
function isSelfTargetOrItem(event, items) {
  if (_TJ2QLEWIcjs.isSelfTarget.call(void 0, event)) return true;
  const target = event.target;
  if (!target) return false;
  return items.some((item) => item.element === target);
}
function getEnabledItems(items) {
  return items.filter((item) => !item.disabled);
}
function itemTextStartsWith(item, text) {
  var _a, _b;
  const itemText = (_b = item.typeaheadText) != null ? _b : ((_a = item.element) == null ? void 0 : _a.textContent) || item.children || "value" in item && item.value;
  if (!itemText) return false;
  return _TJ2QLEWIcjs.normalizeString.call(void 0, itemText).trim().toLowerCase().startsWith(text.toLowerCase());
}
function getSameInitialItems({ activeId, char, items, typeaheadState }) {
  if (!activeId) return items;
  const activeItem = items.find((item) => item.id === activeId);
  if (!activeItem) return items;
  if (!itemTextStartsWith(activeItem, char)) return items;
  const { chars } = typeaheadState;
  if (chars !== char && itemTextStartsWith(activeItem, chars)) return items;
  typeaheadState.chars = char;
  return _I2SBPEXRcjs.flipItems.call(void 0, items.filter((item) => itemTextStartsWith(item, char)), activeId).filter((item) => item.id !== activeId);
}
var useCompositeTypeahead = _TJ2QLEWIcjs.createHook.call(void 0, function useCompositeTypeahead2({ store, typeahead = true, ...props }) {
  const context = _I2SBPEXRcjs.useCompositeScopedContext.call(void 0, );
  store = store || context;
  _TJ2QLEWIcjs.invariant.call(void 0, store, process.env.NODE_ENV !== "production" && "CompositeTypeahead must be a Composite component");
  const onKeyDownCaptureProp = props.onKeyDownCapture;
  const onKeyDownCapture = _TJ2QLEWIcjs.useEvent.call(void 0, (event) => {
    onKeyDownCaptureProp == null ? void 0 : onKeyDownCaptureProp(event);
    if (event.defaultPrevented) return;
    if (!typeahead) return;
    if (!store) return;
    const typeaheadState = getTypeaheadState(store);
    if (!isValidTypeaheadEvent(event, typeaheadState)) return clearChars(typeaheadState);
    const { renderedItems, items, activeId } = store.getState();
    let enabledItems = getEnabledItems(items.length > renderedItems.length ? items : renderedItems);
    if (!isSelfTargetOrItem(event, enabledItems)) return clearChars(typeaheadState);
    event.preventDefault();
    window.clearTimeout(typeaheadState.cleanupTimeout);
    typeaheadState.cleanupTimeout = window.setTimeout(() => {
      clearChars(typeaheadState);
    }, 500);
    const char = event.key.toLowerCase();
    typeaheadState.chars += char;
    enabledItems = getSameInitialItems({
      activeId,
      char,
      items: enabledItems,
      typeaheadState
    });
    const item = enabledItems.find((item2) => itemTextStartsWith(item2, typeaheadState.chars));
    if (item) store.move(item.id);
    else clearChars(typeaheadState);
  });
  props = {
    ...props,
    onKeyDownCapture
  };
  return _TJ2QLEWIcjs.removeUndefinedValues.call(void 0, props);
});
var CompositeTypeahead = _TJ2QLEWIcjs.forwardRef.call(void 0, function CompositeTypeahead2(props) {
  const htmlProps = useCompositeTypeahead(props);
  return _TJ2QLEWIcjs.createElement.call(void 0, TagName4, htmlProps);
});










exports.useCompositeGroup = useCompositeGroup; exports.CompositeGroup = CompositeGroup; exports.useCompositeGroupLabel = useCompositeGroupLabel; exports.CompositeGroupLabel = CompositeGroupLabel; exports.useCompositeHover = useCompositeHover; exports.CompositeHover = CompositeHover; exports.useCompositeTypeahead = useCompositeTypeahead; exports.CompositeTypeahead = CompositeTypeahead;
